import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RequestActionPlanPage } from './request-action-plan';

@NgModule({
  declarations: [
    RequestActionPlanPage,
  ],
  imports: [
    IonicPageModule.forChild(RequestActionPlanPage),
  ],
})
export class RequestActionPlanPageModule { }
